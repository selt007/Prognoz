ѕодготовка локального git репозитори€
ѕодготовить локальный репозиторий можно двум€ способами: создать репозиторий с нул€ с последующим
переносом изменений в удаленный репозиторий и сделать клон удаленного репозитори€.
ѕервый способ создание локального репозитори€ с нул€ командой git init.
—оздадим проект на локальной машине с таким именем:

mkdir your_project

перейдем в этот каталог:

cd your_project

¬ыполним команду git init котора€ инициирует локальный репозиторий:

git init

ƒальше можно добавл€ть файлы в локальный репозиторий.
¬торой способ. —делать на локальной машине клон удалЄнного репозитори€ командой git clone:

git clone https://github.com/you_account/your_project

ѕосле того как мы создали файл его надо подготовить дл€ фиксации и зафиксировать в репозитории, то есть закомитить. 
ѕодготовить дл€ фиксации это означает, что его надо проиндексировать командой git add:

git add *

ѕроиндексированный файл это еще не означает, что он закомичен, это означает, что он готов дл€ коммита в репозиторий, а сам коммит выполн€етс€ командой git commit:

git commit -m "create project"

ƒобавление удаленных репозиториев
≈сли мы создали репозиторий командой git init, то чтобы перенести изменени€ на удаленный репозиторий, 
нам надо его добавить командой git remote add и придумать ему уникальное им€. ¬от как добавл€етс€ удаленный репозиторий:

git remote add origin https://additional_git_address/your_account/your_project

git push origin master